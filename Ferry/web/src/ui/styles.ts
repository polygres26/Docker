import s from './ui.module.css'

/** Class names for table cells and text helpers, for use alongside <DataTable>. */
export const table = { num: s.num, main: s.cellMain, sub: s.cellSub, ellipsis: s.ellipsis, mono: s.mono, narrow: s.narrowCol }
export const ui = { muted: s.muted, mono: s.mono, bullets: s.bullets }
